[project]
name = "every-evening-bot"
version = "0.1.0"
description = "Telegram bot for daily AA step work"
requires-python = ">=3.12"
dependencies = []

[tool.vercel]
entrypoint = "api.stage3:handler"
