[project]
name = "every-evening-bot"
version = "0.1.1"
description = "Telegram bot for daily AA step work"
requires-python = ">=3.12"
dependencies = []

[tool.vercel]
entrypoint = "api.stage4:handler"
